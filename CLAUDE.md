# 给 Claude 的说明

## 和主人沟通
- 主人看不懂代码，请用简单的中文说明做了什么、要怎么看到效果，不要贴代码或讲技术细节。

## 提交方式
- 主人已同意：改动直接提交并推送到 `main` 分支，不要开 PR，不需要主人确认合并。
- 推送前先运行 `npm ci`（如果还没装依赖）和 `npm test`，测试全部通过才推送；测试失败就先修好，不要推坏的代码。
- 改完后提醒主人：在 SillyTavern 里更新「猫猫星绘」扩展，然后刷新页面即可看到效果。
- 如果改坏了，主人说一声就立刻改回去（用 `git revert`，不要改写历史）。
